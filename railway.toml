[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx playwright install chromium --with-deps && node server/index.js"
healthcheckPath = "/api/status"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
protocol = "http"
port = 3000
